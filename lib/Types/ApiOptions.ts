/**
 * The API options.
 *
 * @interface ApiOptions
 */
export default interface ApiOptions {
    endpoint?: string;
    author?: number;
    slug?: string;
    search?: string;
    // Add more if there are more Api classes
}